demo(graphics) # show the beautiful graphs in R
rm(list=ls(all=TRUE)) # remove all objects
ls() #show all objects
options(digits=22) # 7 is the default value in R
getwd() #get your current working dictionary
setwd() #set the working dictionary you want
save.image() #save working space to R.data
library(car) #include the package to your working space
detach() #exclude a database or package
.packages(all.available=TRUE) #秀出有哪些已下載哪些package
some() #隨機看看對象中的一些內容
